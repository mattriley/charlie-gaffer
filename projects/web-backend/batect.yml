project_name: charliemoukbel

containers:
  build:
    image: node:6.10
    volumes:
      - .:/code
    working_directory: /code

tasks:
  shell:
    description: Start a shell.
    run:
      container: build
      command: bash